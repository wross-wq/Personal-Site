import React from 'react';
import * as motion from "framer-motion/client"
import ReactDOM from 'react-dom/client';

export const touchdesigner = () => {
    return (
        <h1>My TouchDesigner Page!</h1>
    );
}